# --- credentials -----------------------------------------------------------
# Belt and braces: the specific file, plus patterns, so a differently-named key
# file can't be committed by accident later.
elevenlabs_api_key.txt
.env
.env.*
*.key
*.pem
*.ppk
id_rsa*
id_ed25519*
*_api_key*
*apikey*
*secret*
*credentials*
authorized_keys

# --- working notes, not for publication ------------------------------------
PLAN.md

# --- build artefacts -------------------------------------------------------
.venv/
build/
__pycache__/
*.pyc

# The Pages catalog is regenerated from packs/ by CI (.github/workflows/pages.yml),
# so it doesn't need committing. Remove this line if you'd rather commit docs/
# and use Pages' "Deploy from a branch" source instead.
docs/

# Intermediate copy of the robot's audio; reference/robot/EN/ holds the files.
reference/robot/audio-en.tar.gz
